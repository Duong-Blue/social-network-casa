spring:
  application:
    name: manager
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      timeout: 1s
      connect-timeout: 1s

  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/appcasa}
    driverClassName: "com.mysql.cj.jdbc.Driver"
    password: ${DBMS_PASSWORD:Duong2k4}
    username: ${DBMS_USERNAME:root}
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: false
    properties:
      hibernate:
        enable_lazy_load_no_trans: false
  jackson:
    serialization:
      fail-on-empty-beans: false
    default-property-inclusion: non_null
  web:
    resources:
      add-mappings: false
  servlet:
    multipart:
      enabled: true
      max-file-size: 200MB
      max-request-size: 500MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: hovanduy1104@gmail.com
    password: lrzv bwar xnwh ybop
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
logging:
  level:
    org.hibernate: WARN
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN
app:
  frontend:
    url: http://localhost:8282
  celebrity-threshold: ${CELEBRITY_THRESHOLD:10000}
minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket-name: ${MINIO_BUCKET:casa}
jwt:
  signerKey: "sahsdhsdauhsuhvdhsuhdsvuhdshdshdvbsdhvbhsdhhsbdhdbhasdiasfiuwenfndcsjvisdivdsidvsaoioisacuihsdafhdshjhdshfsghdjhdsjh"